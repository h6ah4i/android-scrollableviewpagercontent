apply plugin: 'com.android.library'
apply plugin: 'guru.stefma.bintrayrelease'

android {
    compileSdkVersion rootProject.ext.COMPILE_SDK_VERSION

    defaultConfig {
        minSdkVersion rootProject.ext.MIN_SDK_VERSION
        targetSdkVersion rootProject.ext.TARGET_SDK_VERSION
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"

    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

}

dependencies {
    implementation project(':scrollableviewpagercontent')
    implementation 'androidx.appcompat:appcompat:1.0.0'
}

group = 'com.h6ah4i.android.scrollableviewpagercontent'
version = rootProject.ext.LIBRARY_LISTVIEW_VERSION
publish {
    userOrg = 'h6ah4i'
    artifactId = 'scrollableviewpagercontent-listview'
    uploadName = 'com.h6ah4i.android.scrollableviewpagercontent:scrollableviewpagercontent-listview'
    desc = 'ListView binding of ScrollableViewPagerContent library'
    website = 'https://github.com/h6ah4i/android-scrollableviewpagercontent'
}
